# Convert a 32-bit long to an uppercase hex string with ultoa16() (CMOC, Thomson MO5)

Convert an unsigned 32-bit value to a hexadecimal string (uppercase, without a `0x` prefix).

## Steps

1. Include `<cmoc.h>`.
2. Call `ultoa16(val, out)`.

Source: `knowledge/docs/cmoc_h_mo5.md`

# Use delay() to wait in sixtieths of a second (CMOC, Thomson MO5)

Block execution for a duration expressed in 1/60ths of a second.

## Steps

1. Include `<cmoc.h>`.
2. Call `delay(n)`.

## Notes

- `delay(60)` waits about 1 second.

Source: `knowledge/docs/cmoc_h_mo5.md`

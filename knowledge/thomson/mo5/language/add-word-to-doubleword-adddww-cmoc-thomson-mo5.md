# Add a 16-bit word to a 32-bit value with adddww() (CMOC, Thomson MO5)

Add a 16-bit term to a 32-bit value using CMOC's helper.

## Steps

1. Include `<cmoc.h>`.
2. Call `adddww(ptrTo32Bit, term16Bit)`.

Source: `knowledge/docs/cmoc_h_mo5.md`

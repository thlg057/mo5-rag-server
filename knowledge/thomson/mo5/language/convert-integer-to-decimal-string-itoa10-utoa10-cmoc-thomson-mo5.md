# Convert a 16-bit integer to a decimal string with itoa10() and utoa10() (CMOC, Thomson MO5)

Convert a 16-bit value to a base-10 ASCII string.

## Steps

1. Include `<cmoc.h>`.
2. Call `itoa10(val, out)` (signed) or `utoa10(val, out)` (unsigned).

Source: `knowledge/docs/cmoc_h_mo5.md`

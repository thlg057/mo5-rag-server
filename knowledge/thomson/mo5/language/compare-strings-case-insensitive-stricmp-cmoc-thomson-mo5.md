# Compare strings case-insensitively with stricmp() (CMOC, Thomson MO5)

Compare two strings while ignoring letter case.

## Steps

1. Include `<cmoc.h>`.
2. Call `stricmp(s1, s2)`.

Source: `knowledge/docs/cmoc_h_mo5.md`

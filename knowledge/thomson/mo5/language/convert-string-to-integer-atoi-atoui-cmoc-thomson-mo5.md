# Convert a string to an integer with atoi() and atoui() (CMOC, Thomson MO5)

Convert a decimal ASCII string to a signed or unsigned integer.

## Steps

1. Include `<cmoc.h>`.
2. Call `atoi(s)` for a signed result or `atoui(s)` for an unsigned result.

Source: `knowledge/docs/cmoc_h_mo5.md`

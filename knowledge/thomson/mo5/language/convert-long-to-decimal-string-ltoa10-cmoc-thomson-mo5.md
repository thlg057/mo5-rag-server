# Convert a 32-bit long to a decimal string with ltoa10() (CMOC, Thomson MO5)

Convert a 32-bit `long` value to a base-10 ASCII string.

## Steps

1. Include `<cmoc.h>`.
2. Call `ltoa10(val, out)`.

Source: `knowledge/docs/cmoc_h_mo5.md`

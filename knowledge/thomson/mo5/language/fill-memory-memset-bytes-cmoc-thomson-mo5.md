# Fill memory with memset() using an 8-bit value (CMOC, Thomson MO5)

Fill a memory region with a repeated 8-bit value.

## Steps

1. Include `<cmoc.h>`.
2. Call `memset(ptr, byteValue, nBytes)`.

Source: `knowledge/docs/cmoc_h_mo5.md`
